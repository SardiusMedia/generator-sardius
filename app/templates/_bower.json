{
  "name": "<%= _.slugify(_.humanize(appname)) %>",
  "version": "0.0.0",
  "dependencies": {
    <% if(filters.bootstrap) { %><% if (filters.sass) { %>
    "bootstrap-sass-official": "~3.1.1",<% } %>
    "bootstrap": "~3.1.1",<% } %>
    "font-awesome": ">=4.1.0"
  },
  "devDependencies": {

  }
}
